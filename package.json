{
	"name": "predator-prey-simulation-server",
	"version": "1.0.0",
	"description": "Simple server for serving the client.",
	"license": "UNLICENSED",
	"main": "server.js",
	"scripts": {
		"client": "npm start --prefix client",
		"install-client": "cd client && npm install && cd ..",
		"install-server": "npm install",
		"install-all": "npm run install-server && npm run install-client"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/jozefmery/Project-Boids.git"
	},
	"author": "Jozef Méry",
	"homepage": "https://github.com/jozefmery/Project-Boids",
	"dependencies": {
		"express": "^4.17.1"
	}
}
